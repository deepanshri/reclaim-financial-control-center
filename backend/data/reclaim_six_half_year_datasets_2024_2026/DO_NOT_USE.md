# ARCHIVED — DO NOT USE

This folder is a leftover duplicate dataset from earlier generator work.

It is **not** loaded by the Reclaim API.

Authoritative synthetic data:

`backend/data/reclaim_six_half_year_datasets/`

Do not point `data_loader` or `data_repository` at this tree.
Do not treat values here (including historical 286875.00 fee impacts) as financial truth.
